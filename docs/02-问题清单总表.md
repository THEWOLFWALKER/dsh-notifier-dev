# dsh-notifier 问题清单总表

```yaml
审查对象: dsh-notifier v0.9.0 (commit 1415d7d)
宿主参照: deepseek-harness (vendor/cordis 4.0.1)
竞品参照: dsh-im v3.1.1 (xmanrui/dsh-im)
汇总日期: 2026-08-28
审查轮次: 三轮定向(安全/功能/耦合) + 三轮渠道协议(对照官方SDK源码) + 一轮零提示黑箱
           + 四轮专项(A时序并发 / B数据持久化 / C宿主契约 / D极端工况)
           + 四轮补充(E文本编码·时间时钟·数字解析 / F测试mock保真·配置矩阵·文档一致性
           / G错误处理·幂等重试·进程生命周期 / H竞品dsh-im深度对照)
           + 四轮终审(I入站路由状态机·命令矩阵·时间数字 / J审批链路·提问链路·回执文案
           / K适配器载荷·配置双轨·测试保真 / L竞品能力对照·宿主契约·文档一致性)
           累计 20 轮 · 60 次子代理派发
协议仲裁源: dingtalk-stream 2.1.4/2.1.6-beta.1/2.1.7-beta.1 全系、qqbot-nodejs 1.0.4、
           node-sdk 1.73.0、宿主 vendor/cordis 源码、飞书/QQ/TG/钉钉官方 API 文档
测试基线: 裸 node --test 1381 项·1 失败(scripts/test-channel.mjs 被 Node22 默认 glob 拾取,见 G-57);
           G-57 已修复: 脚本重命名 scripts/channel-selfcheck.mjs,该拾取项消失;
           除该拾取项外 1380/1380 通过(上轮"1352/1352"为 test/ 子集口径)
统计: P1×6 · P2×18 · P3×41 · 安全中危×7 · 安全低危×6 · 结构性×1 · 信任项×1 = 共 80 项
条目格式: 问题定位 / 易错辨析 / 竞品参照
```

## 修复核销（2026-08-28 终局对账）

| 发布线 | 批次 | 已修条目 | 提交 |
|---|---|---|---|
| v0.9.1 (R1) | W1-W5 | G-01/02/10/23/24/42 · G-03/22/40 · G-04/06/25/33/43/52/65 · G-05/41 · G-51/48/49（18 项） | `45a6b76` 起 R1 系列 |
| v0.9.2 (R2) | W6-W8 | G-50/08/09/56 · G-53/54 · G-11/55/29/07/21/12（13 项） | `27fc985` `24f2fd5` `b872805` `3fddb29` |
| v0.9.3 (R3) | W9 | S-02/05/06/07（4 项） | `a331566` |
| v0.9.4 (R4) | W10 | G-13/S-12/G-61/62/63/64/G-32/G-38/G-39/G-45/G-28（11 项） | `5a06dac` |
| v0.9.4 (R4) | W11 | G-15/G-46 · G-16/17/18/26/27/30/31/34（10 项）；G-19 取证登记不改码（`docs/memory/risks.md`） | `6036706` `01c20ec` |
| v0.9.4 (R4) | W12 | G-20/47/44/14 · S-14/S-04（6 项） | `28cbd75` |
| v0.9.5 (R5) | W13 | G-57/58/59/60 · S-11/S-13（6 项）+ mock 分层原则 | `083135f` |
| 合计 | | 已修 65 项 + G-19 取证登记（待真机） | |

## 总览

| 类别 | 数量 | 编号段 |
|---|---|---|
| P1 功能缺陷 | 6 | G-01 ~ G-06 |
| P2 功能/联动/协议缺陷 | 18 | G-07 ~ G-20, G-50 ~ G-53 |
| P3 健壮性/一致性 | 41 | G-21 ~ G-49, G-54 ~ G-65 |
| 安全 · 中危 | 7 | S-02 ~ S-08 |
| 安全 · 低危 | 6 | S-09 ~ S-14 |
| 安全 · 结构性 | 1 | S-01 |
| 信任/信息 | 1 | S-15 |

---

## 一、P1

### G-01 · 钉钉 Stream ack 回执三重偏差

- **定位**：`src/inbound/dingtalk-stream.mjs:219-227,307` —— `ackFrame(String(frame.messageId))` 读顶层 messageId；回执头用 `requestId` 字段名；data 未字符串化且缺 `message:'OK'`
- **易错辨析**：三处偏差叠加（读错路径 + 头字段名错 + 载荷形状错），单看每一处都"像个能跑的帧"，只有对照官方 `client.mjs` 才能确认服务端全不认。最容易看漏的是头字段名：官方回执头就是 `messageId`，不是 restful 惯用的 `requestId`——直觉写法恰好是错的。测试 mock（`test/inbound.dingtalk.test.mjs:153-156,295`）把顶层 messageId 形态钉死成契约，是 1352 测试全绿仍漏检的直接原因
- **竞品参照**：dsh-im 直接用 `dingtalk-stream` 官方 SDK，ack 帧构造在 SDK `client.mjs` 内，无此问题——凡官方 SDK 覆盖的协议点，竞品零成本继承正确性

### G-02 · 钉钉 SYSTEM/ping 探活帧零应答，SYSTEM 帧族整体未处理

- **定位**：`src/inbound/dingtalk-stream.mjs:303-316` —— `handleFrame` 无 `frame.type==='SYSTEM'` 分支，ping/disconnect/KEEPALIVE/REGISTERED 全部落入业务消息路径后静默丢弃
- **易错辨析**：极易误判为"WS 协议层 ping/pong 自动处理了，不用管"——但官方 SDK 对 SYSTEM ping 是应用层回显帧（headers+data 原样返回，data 含必须回显的 opaque），与传输层心跳是两码事。漏答的表现是周期性掉线重连，日志只有"重连"没有根因，排障方向容易被带偏到网络层。与 G-01 是同根：裸协议实现丢掉的都是 SDK 隐式处理的协议点
- **竞品参照**：同 G-01，官方 SDK `WSClient` 内置 ping 应答与 SYSTEM 事件分发

### G-03 · 微信 iLink 发送按 UTF-16 码元切块，emoji/生僻字被切半

- **定位**：`src/channels/wechat-ilink/legacy-core.mjs:374-386` —— `for (i += size) content.slice(i, i+size)` 按码元切；同款问题 `src/inbound/qq-gw.mjs:422,460` 两处
- **易错辨析**：JS 字符串 length/slice 全是 UTF-16 语义，纯中文测试永远测不出（BMP 内字符 1 码元=1 码点），必须 emoji（星体平面）跨块边界才炸——切出孤立代理项后微信端显示乱码或拒收。最讽刺的辨析点：`src/inbound/segment.mjs` 里码点安全分段已存在（`Array.from`），Telegram 侧也有同款代理对保护，唯独微信发送路径没复用——不是不会写，是没意识到这里是同一个问题
- **竞品参照**：dsh-im 的 `safeSliceIndex`（切点落在代理对回退一位）正是此问题的标准解法，说明竞品真机踩过此坑

### G-04 · `/stop 任意文字` 被误判为取消命令

- **定位**：`src/inbound/conversation.mjs:521-525` —— `trimmed === '/stop' || trimmed.startsWith('/stop ')` 前缀匹配即取消
- **易错辨析**：典型的前缀命令匹配陷阱：`/stop 一下别急`、`/stop 等等再发` 误触发 `agent.cancel()`，且参数静默忽略、无确认无回滚——用户一句话把长任务杀了。I 轮终审修正：`/stopwatch` 不中招（`startsWith('/stop ')` 要求命令后紧跟空格）——审查此前缀时容易同时高估与低估其杀伤面，真正命中的是"精确 `/stop`"与"`/stop `+任意尾巴"两种形态
- **竞品参照**：dsh-im 命令集更小（无 /stop 类高危命令），此坑未踩；其命令解析全部精确匹配

### G-05 · 管理台 settleQuestion 审计写失败把成功裁决变成 500

- **定位**：`src/admin/api.mjs:1204-1253` —— `adminSettle` 裁决生效后 `appendAudit` 在 try 块外执行，磁盘满/权限异常直接抛 500
- **易错辨析**：业务状态与可观测性副作用的顺序问题——裁决已生效但响应报错，前端引导用户重试，重试命中 already-resolved 再报错，内外状态认知分叉。易错点：审计"只是记日志"的直觉让人把它留在主 try 里，恰恰忘了它有 IO 就会抛
- **竞品参照**：dsh-im 无管理台裁决面，不存在对应路径

### G-06 · 命令解析不支持 `/cmd@botname` 变体

- **定位**：`src/inbound/commands.mjs:25-31` —— `parts[0].slice(1)` 不剥 `@botname` 后缀；TG/钉钉入站也无 @ 剥离
- **易错辨析**：Telegram 群聊客户端自动补全 `/cmd@mybot` 是平台标准行为，单聊测试永远复现不了——群聊里 `/whoami` 全部变成普通消息进 agent。易错点：命令解析单元测试都是理想形态输入，真实平台输入的形态差异只有真机群聊才见
- **竞品参照**：dsh-im 主要做单聊桥接 + 官方 SDK（SDK 的 command handler 自带 @ 剥离），未显式处理也不踩坑

---

## 二、P2

### G-07 · QQ 网关关闭码无语义处理 + token 永不主动失效

- **定位**：`src/inbound/qq-gw.mjs:404-407` —— close 一律 resume+退避不看 code；全文件无 `tokens.invalidate()` 调用
- **易错辨析**：`invalidate()` 在 `_tokens.mjs` 里存在且正确，但 QQ 网关侧从未调用——token 被平台提前吊销（close code 4004）时，带着同一缓存 token 无限重连直到 TTL 自然过期。易错点：重连逻辑本身是对的（RESUME/退避都对），"重连正确但带着死凭证重连"这个组合只有对着官方 SDK 的关闭码分支表才能看出来。官方 SDK 语义：4004 刷 token、4008 固定等 60s（限流码走短退避反而雪崩）、4006/4007/4009 弃会话重 IDENTIFY
- **竞品参照**：dsh-im 用 `qqbot-connector` SDK，reconnect.ts 的关闭码分支表全量继承

### G-08 · Telegram 429 不读 retry_after

- **定位**：`src/inbound/telegram-bot.mjs` `api()` 错误路径——grep `429|retry_after` 零命中
- **易错辨析**：TG 的 429 响应带 `parameters.retry_after`（秒），固定短退避在限流期是连续撞墙。易错点：退避机制存在（`errorBackoffMs` 默认 5s）给人"已处理限流"的错觉，但 5s 与平台要求的 N 秒不匹配时越撞越死
- **竞品参照**：dsh-im 用 grammY/telegraf 系 SDK，框架内置 flood control（429 自动等待）

### G-09 · Server酱 SC3（sctp 前缀 key）不支持

- **定位**：`src/adapters/serverchan.mjs:9,22` —— 固定 `sctapi.ftqq.com` Turbo 域名
- **易错辨析**：SC3 用户的 key 打 Turbo 端点直接 404，报错文案完全不提代际问题——用户第一反应是 key 错了去重置，越弄越乱。易错点：Turbo API 仍存活，配置校验全过，只有发送时 404，且 404 文案无代际提示
- **竞品参照**：dsh-im 按 key 前缀分流 sctp/ft07 域名，SC3 开箱可用

### G-10 · 钉钉建连 body 用 `uesrAgent`，官方 SDK 全系用 `ua`

- **定位**：`src/inbound/dingtalk-stream.mjs:201` + 头注释:9-10（"uesrAgent 是官方 SDK 的拼写错误字段名，服务端认的就是它"）
- **易错辨析**：本条是审查过程自我纠错的样本——终审报告曾采信注释判定"勿改"，后拉取 2.1.4/2.1.6-beta.1/2.1.7-beta.1 三个版本源码逐行核对：建连请求全部是 `ua: this.config.ua`，无任何版本存在 `uesrAgent`。注释的"官方拼写错误"说法查无实据。当前不致故障（建连靠 clientId/clientSecret 鉴权，未知字段大概率被忽略），但注释会误导后续维护者把错误字段名当协议事实传承
- **竞品参照**：dsh-im 官方 SDK 建连，字段名天然正确

### G-11 · `_tokens` invalidate 不清 inflight，旧任务晚完成把失效 token 写回缓存

- **定位**：`src/adapters/_tokens.mjs:17-35` —— `invalidate()` 只置 `cached=null`；在飞的旧 inflight IIFE 完成时无条件执行 `cached = {...}`，可能覆盖新 inflight 刚写入的新 token
- **易错辨析**：本条经 A 轮专项复核修正过一次描述——`get(force=true)` 并不 join 旧 inflight（`if (!force && inflight !== null)`），所以"重试拿到旧任务结果"的原表述不准。真实竞态是写回覆盖：token 被平台提前吊销（钉钉 errcode≠0 / 企微 40014）→ invalidate → 新 inflight 取新 token → 此前在飞的旧 inflight（吊销前发起，拿到的正是被吊销的 token）晚一步完成 → 把坏 token 连同 `expiresAt=now+7200s` 写回 cached → 理论上此后两小时都用死 token。触发窗口极窄（需要 invalidate 前一瞬恰有 get 在飞且完成更慢），但后果与窗口不成比例。钉钉 `postJsonWithToken` 的"作废重取重试一次"能当场自愈，真正受伤的是同一 manager 的后续调用方
- **竞品参照**：dsh-im 各官方 SDK 自带 token 缓存（SDK 内部有 generation/版本号防写回竞态）

### G-12 · 微信 iLink 轮询假死无应用层检测

- **定位**：`src/channels/wechat-ilink/legacy-core.mjs:273-325` —— 轮询循环自管失败计数，无"连续零消息超时"监督
- **易错辨析**：TCP 活着但服务端不再推消息的假死，现有熔断（只覆盖 sendmessage 限流）和失败计数（只覆盖显式异常）都不触发——通道静默失效，用户以为插件挂了。易错点：有重连有退避有熔断的代码看起来"韧性完备"，三类保护保护的都不是这个故障模式
- **竞品参照**：dsh-im 有通用 supervisor（15s 对账 + 退避序列 + 防重入），假死可检测

### G-13 · 渠道枚举 4 份硬编码

- **定位**：`src/admin/api.mjs:35`、`src/inbound/identity.mjs:19`、`src/inbound/target-guard.mjs`、`src/assembly/inbound-channels.mjs:19-26` —— 同一"支持哪些入站渠道"事实 4 份运行时拷贝
- **易错辨析**：新增通道漏改任何一处，症状各不相同（admin 422 / 绑定静默无效 / 形态守卫 fail-open），没有一个报错指向"枚举不同步"——排查时极易当成新通道自身的 bug。这是横切常量的经典散落问题
- **竞品参照**：dsh-im 渠道面窄且用 SDK 常量，未形成此债

### G-14 · 出站配置"视图热、投递冷"

- **定位**：`src/admin/api.mjs:899-924`（putChannel 只写 store）+ `src/index.mjs:203,618-662`（投递层持装配时快照）
- **易错辨析**：管理台保存后 UI 即时回显新配置，但 notifier 到重启才用——"改了没生效"与"改了生效了"两种体验在同屏并存（对照组成员/绑定写后半秒热生效）。易错点：不是 bug 而是语义分裂，但 UI 无任何"待重启"标记，用户无法区分
- **竞品参照**：dsh-im 配置变更走全量重建（牺牲瞬断换一致性），不存在双速语义

### G-15 · bus 消息消费优先级 = 装配注册顺序

- **定位**：`src/inbound/bus.mjs:173-180` + `src/questions/router.mjs:788-789` 注释（"审批编号回复 > 提问编号回复 > 普通对话"仅存于注释）
- **易错辨析**：核心优先级契约完全由 `index.mjs` 装配顺序 + 注释维系，重排装配块不报任何错，「1」的归属静默改变。易错点：这是"约定优于配置"的暗契约，测试若按现装配顺序写 mock 也测不出来
- **竞品参照**：不适用（dsh-im 无审批/提问双链争抢问题）

### G-16 · 审批 waiter 纯内存，与持久化 ledger 生命周期可分叉

- **定位**：`src/inbound/bus.mjs`（waiters Map 仅内存）+ store ledger（`ap:` 前缀行跨重启存活）
- **易错辨析**：热重载/崩溃重启后 pending 账本行还在而 waiter 已死，用户点旧卡片收到「已处理」误导回执，宿主侧等待的审批 promise 永远等不到答案（直到自身超时）。易错点：两边各自看都"正确"（账本持久化对、waiter 内存态对），分叉只在生命周期边界出现
- **竞品参照**：不适用（竞品无远程审批）

### G-17 · Telegram/飞书旧卡片兼容放行窗口

- **定位**：`src/inbound/telegram-bot.mjs:240-257`（refs TTL 15min 兜底）、`src/inbound/feishu-bot.mjs:379-395`（无等价 TTL 约束）
- **易错辨析**：升级前在途卡片缺来源会话元数据，warn 后放行——TG 有 refs TTL 兜底，飞书侧没有，同一策略两渠道强度不同。窗口内卡片被转发后点击可跨会话裁决。易错点：这是兼容性让步，有 warn 有理由，但强度不对称容易被当成"飞书也受了保护"
- **竞品参照**：不适用（竞品无按钮卡片裁决）

### G-18 · 事件去重键 `session.id:event.seq` 吞掉宿主重放

- **定位**：`src/event-listener.mjs:426-427` —— dedup 对所有 intent 生效（含 approval/asked 即时推送）
- **易错辨析**：宿主重放同 seq 事件时，第二次真实需要的审批提醒被静默吞掉。易错点：去重键对"每 turn 一条"类事件完美，对"同 seq 多负载"类事件（重放携带不同 payload）就错——键粒度对不同事件类型的适配性差异容易被一个统一键掩盖
- **竞品参照**：不适用（竞品不订阅宿主事件流）

### G-19 · `selectHostEventContext` 猜测根上下文（C 轮加证）

- **定位**：`src/host-events.mjs:42-48` —— scoped child 时主动切 root 订阅
- **易错辨析**：C 轮专项拿到了 cordis 源码级证据（`vendor/cordis/src/events.ts:165-175` 事件服务按 context filter 过滤 listener、`core/agent/src/runtime-types.ts:175` 声明 `agent/disposed` 为 Scoped 事件）：root 订阅对 scoped 事件可能漏收（通知闭环丢审批/报错提醒）或听到不该听的（隔离失效）。易错点："往 root 挂监听最稳"是直觉，恰好与 cordis 的 scoped 过滤机制相悖；插件侧靠 `payload.agent ?? payload` 的宽容解析部分掩盖了丢事件症状
- **竞品参照**：不适用（宿主生态内无同类插件可比）

### G-20 · `pairing.mint()` 双次落盘崩溃窗口

- **定位**：`src/inbound/pairing.mjs:205-241` —— 先落 minted → audit → 改 active 再落盘
- **易错辨析**：两写之间崩溃，盘上残留 minted 未下发的孤儿码（TTL 内仍可被兑换）。易错点：两次写各自原子，但"状态推进"整体非原子——单写原子性给了人事务完整的错觉
- **竞品参照**：不适用（竞品无配对码机制）

### G-50 · 出站超时盲目重试形成 at-least-once 重复通知（E~H 轮新增）

- **定位**：`src/routing.mjs:48-60,98-119`（timeSensitive `attempts:3` + 指数退避）+ `src/adapters/_shared.mjs:75-99`（`postJson` 超时统一抛 `TIMEOUT`，不区分"已送达未应答"）
- **易错辨析**：超时的三种真况——连接失败（重试安全）/ 服务端已收到但响应慢（重试=重复投递）/ 响应丢失（同前）——被折叠成第一种。分段部分成功已有 `noRetry` 短路（`notify.mjs:44-52`），说明作者意识到"重复轰炸"这个面，但"整条超时"这一支仍盲目重发；timeSensitive 默认 3 次尝试，用户最坏收 3 份同一通知。易错点：只有无幂等键的 HTTP 渠道受害——QQ 出站已用冻结 `msg_seq` 自愈（`qq-bot.mjs:65-89`），而钉钉/飞书/TG/webhook/企微全裸奔；且测试 mock 的 fetch 不会出现"超时但已送达"，1352 绿测结构上永远测不出
- **竞品参照**：dsh-im 文件投递把此类失败标 `uncertain: true` 并拒绝盲目重试（`feishu-channel.mjs:359-366`），错误体系显式区分"投递结果未知"与"确认失败"

### G-51 · 合并窗按 `channel:userId` 聚合，同用户跨 chat 消息串台合并（I 轮）

- **定位**：`src/inbound/conversation.mjs:475,477,566-591` —— pending 键与 flush 键均为 `${channel}:${userId}`，不含 chatId 维度；flush 采用最后一条消息的 envelope
- **易错辨析**：注释与键构造都写 `${channel}:${userId}`，把"用户"当成了聚合边界——但 envelope 还有 chatId：同一 Telegram 用户在私聊与群（或两个群）窗口内先后发言，全部并成一条，按最后一条的 chat 投递与回执。route() 拿 merged 文本走 resolveTarget，路由对了但内容已串台；"一人一窗"的单会话测试永远测不出，这是键粒度少一维的静默错聚合
- **竞品参照**：dsh-im 按平台消息原生 chat/会话对象隔离（SDK 事件对象自带 chat 维度），无跨会话合并面

### G-52 · 裸编号多选拒绝逗号后空格，答案被当普通文本投给 agent（J 轮）

- **定位**：`src/questions/router.mjs:705` —— `/^\d{1,2}([,，]\d{1,2})*$/` 不容 `1, 3`（逗号+空格）、`1、3`、`1 3`；不匹配即 `return false` 落回对话路由
- **易错辨析**：正则已兼容全角逗号却不容半角逗号后一个空格——恰是手机输入法最常见形态；被拒后不裁决、不提示，"1, 3" 作为普通消息进 agent 上下文（答案丢失+语境污染双害），用户以为已作答。测试只喂理想形态 `1,3`；单选场景掩盖多选解析面
- **竞品参照**：dsh-im `src/channels/shared/harness-question.mjs:70-81` 用 `split(/[,，、;；\n]+/)` 逐 token trim，多分隔符全兼容

### G-53 · 错误文案无"公开码/内部细节"分层，HTTP 状态、响应体、网络原文直达用户（J 轮）

- **定位**：`src/adapters/_shared.mjs:68,95,125,146,172`（`${channel}返回 HTTP ${status}`+200 字符响应体、`${channel}请求失败: ${error.message}`）+ `src/notify.mjs:89,154`（`failed[].error` 原样透传进事件/工具反馈）+ `src/health.mjs:27,37`（detail 直传 admin UI）
- **易错辨析**：channel 是小写适配器键（用户看到 "qq-bot返回 HTTP 403"）；fetch 底层错误 message 可能携带代理地址/证书路径/机器路径；响应体片段进文案后随事件线直达 LLM 与前端。三层各自透传、无一处归一为稳定错误码——不是单点遗漏，是体系缺失，1352 绿测不查文案内容故永不暴露
- **竞品参照**：dsh-im 有 `publicMessageFailure` 归一层，测试显式断言 providerDetail/stack/secret 形态不进公开文案（`test/message-failure.test.mjs:62-73`、`test/client-last-message-error.test.mjs:64-69`）

---

## 三、P3

| 编号 | 问题定位 | 易错辨析 | 竞品参照 |
|---|---|---|---|
| G-21 | `qq-gw.mjs:384-386` INVALID_SESSION 不看 `d` 可恢复标志,一律弃会话多付一次 IDENTIFY | 官方 SDK 按 `d:false` 才弃;一律弃的代价只是多一次握手,易被当"稳妥"而忽略 | SDK 继承正确分支 |
| G-22 | `qq-gw.mjs:422,460` QQ 两处 slice 同 G-03 码元切根;被动回复条数配额(c2c 4/群 5)无意识 | 与 G-03 同根同修;配额超限平台侧静默丢弃,无报错可查 | 竞品用 SDK 发送,边界由 SDK 管理 |
| G-23 | `dingtalk-stream.mjs` richText 回调未归一(picture/image 已覆盖) | 富文本里混排文本+图的会话,文本部分丢失;纯文本/纯图测试不暴露 | SDK 抛出原始 payload,竞品自行解析同样需补 |
| G-24 | `dingtalk-stream.mjs:433-439` 被动回复 messageId 用 hash6 合成,同会话同内容两次回复同 ID | 上游若以 messageId 幂等会吞第二次;属 G-46 hash6 族的出站支 | 不适用 |
| G-25 | `feishu-bot.mjs:47-56` @提及只删 `@_user_N` 占位符,用户名语义丢失且留双空格;J 轮补证命令支:`/pair @名字 code` 的 @残片原样进 args | 飞书事件带 mentions 映射可还原 `@名字`,只删占位符是"看起来剥干净了";命令参数侧同根但症状不同(配对码被 @残片污染) | 竞品用 SDK 事件对象,mention 还原现成 |
| G-26 | `feishu-bot.mjs:253-255` 非文本消息以 `[不支持的消息类型：x]` 注入 agent 会话 | 占位符文本可被 agent 当指令语境解读(注入面);静默忽略+回执更稳 | 不适用 |
| G-27 | `wxpusher-callback.mjs:146-169` 回调无 messageId,合成 `uid:time:hash6(content)` | WxPusher 无签名无 msgId 属平台限制,合成键是唯一选择,但同秒同内容两条真实消息被吞——键粒度与业务语义(重发也可能是有意的)冲突 | 竞品未接 WxPusher |
| G-28 | `_ilink-api.mjs:160-171` contextToken 缺省静默省略字段 | 可能落错会话窗口(代码自认限制);静默无 warn,排查时不可见 | 竞品无多窗口支持,不存在此维度 |
| G-29 | `_tokens.mjs:18` refreshMarginMs(60s) 与 expiresInMs 比较无钳制 | expiresIn<60s 的渠道(理论存在)永判不新鲜;当前渠道全是 7200s 不触发,属休眠边界 | SDK 内部按比例 margin |
| G-30 | `commands.mjs:140-163` locked-out 文案固定"连续错 5 次",过期码反复提交也走到锁出 | 过期≠错误,锁出文案误导用户以为码泄露 | 不适用 |
| G-31 | `pairing.mjs:275-285` 过期码计入失败计数,误输 5 次过期码锁 10 分钟 | 与 G-30 同族:过期与错误混计,惩罚面过宽 | 不适用 |
| G-32 | `_bounded.mjs:24-49` `max` 传非数字时 `Math.max(1,NaN)` 回落 1 而非默认上限 | NaN 静默变 1 是"防御代码引入新边界"的反例;调用方全传常量,休眠中 | 不适用 |
| G-33 | `conversation.mjs` /agent use 只取 args[1],workspace 名含空格被截断 | needle 匹配失败回执"未找到",用户不知道是空格截断;需 join 全部剩余参数 | 不适用 |
| G-34 | `event-listener.mjs:262,404`+`approval/router.mjs` 双路径通知:事件线文本与交互线卡片并行,绑定双渠道用户每次收 2 条 | 卡片成功后无同 key 抑制;是设计叠加还是缺陷需产品定性,现状无抑制无文档 | 不适用 |
| G-35 | `assembly/inbound-channels.mjs:8-13` facade 迁移半途:telegram/feishu/wechat 走 facade,qq/wxpusher/dingtalk 直连裸工厂 | 两套风格并存,新贡献者不知以哪套为模板;legacy-core 标注了过渡但 facade 未补齐 | 不适用 |
| G-36 | `admin/scan.mjs:25-29` 管理面直连 inbound 协议内部件(`_qq-scan`/`_ilink-api`) | 跨层依赖让协议层无法重构;扫码能力应上提为渠道 facade 的 admin 面 | 不适用 |
| G-37 | `index.mjs`(扇出 39)+`config.mjs`(扇出 15):index 内联装配逻辑;config 同时是解析器+注册表双轨 | 耦合审查已知:改动放大面大,ADAPTERS/SECRET_FIELDS 应独立 registry | 竞品单层结构无此债 |
| G-38 | `spec-channels.mjs` slack 仅 Incoming Webhook,Bearer 配置会失败 | spec 声明的字段集与 README 未声明边界,用户按官方 bot token 配置即失败且报错不指向原因 | 竞品未接 slack |
| G-39 | `spec-channels.mjs` discord content 无 2000 字符显式 fail-fast | 默认 1200 码点分段兜住大半,关分段或调大 maxCodepoints 时暴露 400 | SDK/framework 层边界 |
| G-40 | `qq-gw.mjs:73-76` stripMention 正则假定 `<@!ID>`/`@名字` 前缀形态 | 平台改格式即漏剥,@残留进 agent 语境;需与 fixtures 真实样本回归 | SDK 解析 mention |
| G-41 | `approval/router.mjs:347-351` 接收人校验被 `targets.length > 0 &&` 门控:pushedTo 空时跳过 userId 比对 | 黑箱轮发现:增量落账窗口/写盘失败可造成空表,fail-open 形态违反宪法#7;利用前提是持有 HMAC token(只随卡片下发),属纵深缺口而非可利用洞 | 不适用 |
| G-42 | `dingtalk-stream.mjs:311-313` data 二次 parse 失败静默 return:ack 已发服务端不重推,消息不可恢复丢弃且无 warn | 先 ack 后处理的代价面:处理链任何一步静默失败=永久丢消息;无 warn 使丢消息不可观测 | SDK 解析失败会走异常回调 |
| G-43 | `questions/router.mjs:709-724` 缺 chatId 的 fail-closed 路径消费裸编号后无任何回执;I 轮补证:chatId=null 时按 (channel,userId,accountId) 命中 exact/onChannel,同用户任意无 chatId 信封的裸编号均被吞 | 用户不知道回复为何被吞;"消费但不裁决"对系统是对的,对用户是黑洞 | 不适用 |
| G-44 | `identity.mjs:63-68` 坏绑定键读取时丢弃但永不写回清理 | 存储与业务视图长期不一致,影响 owner 计数与迁移判断;读时清洗不落盘=每次启动重复清洗 | 不适用 |
| G-45 | `_shared.mjs:152-176` postText 未设 `content-type: text/plain`;当前全 spec 为 encode:'json' 零调用方 | 死代码休眠隐患:未来接入文本协议渠道时无默认头,415/语义错位不指向原因 | 不适用 |
| G-46 | hash6 合成 messageId 族:微信 `protocol.mjs:93`(平台缺 rawId 兜底 `wx:acct:uid:hash6(text)`)+G-27 wxpusher+G-24 钉钉出站 | B 轮确认微信兜底键直接进 bus 24h 去重:同用户 24h 内重发相同文本(审批场景连回两次「1」)第二次被静默吞;微信兜底仅在平台缺 rawId 时生效,触发频率取决于协议行为,wxpusher 支路必然合成。辨析:合成键意图是吸收重推,副作用是吞合法重复——「重复」在 IM 语境是用户有意的 | 竞品幂等全用平台原生 msgId(SDK 提供),不合成 |
| G-47 | `agent-router.mjs` 直写 `route:sessions` 的行无清理路径 | B 轮确认:session-registry.sweep 只清有 `disposedAt` 的行;经 /quiet、管理台设置的出站覆盖行(无 disposedAt 字段)永存,长期运行 state.json 单调膨胀;反向辨析:若 registry 后跟踪同会话并 disposed,整行被 sweep 删掉时用户的出站覆盖配置一并丢失 | 竞品会话表短生命周期(桥接即删) |
| G-48 | `conversation.mjs:222-237,395-408` /bind 与 /agent use 覆盖旧绑定时不 `detachInbound(旧sid)` | C 轮发现:旧会话的 registry inbound 挂钩残留——同一 channel:userId 挂两个会话,/route 展示误导且可能影响兜底路由判定;attachInbound 有去重(同目标不重复)但不摘旧目标,易误以为去重即完整 | 不适用 |
| G-49 | 身份/路由键归一族:`identity.mjs:115-120` allows 不 trim userId(addBinding 写入时 trim)、bind 键 `conversation.mjs:101`/`agent-router.mjs:213-216`/registry 三处均无统一 trim/大小写策略 | B/C 轮合并发现:四处键构造各写各的,上游 envelope 若带空白 userId 即分裂为多键(绑定表命中而 allows 未命中);当前各通道适配器恰好都输出归一 id,休眠边界。userId 含冒号已被显式拒绝(D2 轮验证 identity.mjs:205-209),冒号注入面已封 | 竞品以 SDK 事件对象的 canonical id 为键,天然唯一 |
| G-54 | `telegram-bot.mjs:217-219`+`feishu-bot.mjs:471-476`(J轮) 按钮失败统一回"该审批已处理或已过期(token 单次核销)",而 bus.decide 实际还有 key-mismatch/source-chat-mismatch/token-required 等原因;TG `query.message` 缺失(卡片消息被删)同样落"请到原会话操作" | N 种校验失败折叠成一种话术:用户以为已裁决或 token 过期,排障方向被带偏;文案层与错误码层没对齐是"最后一公里"常见漏点 | dsh-im 提交失败区分"提交失败请重试"/"只有发起用户可处理"等细分话术 |
| G-55 | 数值钳制方向族(I轮):`_tokens.mjs:23` Math.max(1000,expiresInMs) 把 0/负钳成 1 秒 TTL;`qq-bot.mjs:59` \|\|7200 把 0 吞成 7200;`wecom-app.mjs:73` ??7200 保 0 再被钳 1 秒;`_feishu-register.mjs:161` Math.max(0,Number(x)\|\|0) 非法变 0=立即超时 | 同一异常值四种走向(变下限/变默认/变 0/保留 0),token 三层链上同值不同命:0 在 qq 层活 7200s、在 wecom 层只活 1s;单看每处都"像防御",合起来互相矛盾;当前各平台恒返 7200 故休眠 | dsh-im registration-manager.mjs:53-57 对非有限数/≤0 直接抛错,不拿默认值覆盖 |
| G-56 | `qq-bot.mjs:83-87`(K轮) 成功路径 response.json() 失败被 catch 吞掉,"2xx 非 JSON 也视为成功" | 代理/网关返回 200+HTML(登录页/错误页)时被记成功:用户永远收不到通知且日志无失败痕迹;注释自认该语义,属有意为之但连 warn 都没有 | dsh-im telegram 层非 JSON 抛 telegram-response-invalid(telegram-api.mjs:374-397) |
| G-57 | `package.json` test 脚本裸跑 `node --test` 时 Node 22 默认 glob `test-*` 拾取 `scripts/test-channel.mjs`(CLI 无参即退出非零):实测 1381 项 1 失败;上轮"1352/1352"为 test/ 子集口径 | 基线口径不固定,"全绿"失去意义:同仓库两种跑法两个结论;CLI 脚本命名 test-* 撞测试 runner glob 是纯命名陷阱 | 竞品 CLI 置 bin/ 目录不进 test glob;W13 已修复:重命名 scripts/channel-selfcheck.mjs 并同步 README/AGENTS/docs/CI 引用 |
| G-58 | `test/inbound.dingtalk.test.mjs:19-50,137-156,295-304` mock fetch 硬编码 4 路由+固定 srv_N 帧形状;`test/inbound.qq.test.mjs:43-69` FakeWebSocket 仅 open/message/close 无 error/超时/半帧;`test/public.test.mjs:34-43` fetch mock 无超时支路 | G-01 的泛化:mock 与实现同源共生,协议漂移时 mock 先"对了";WS 异常路径全靠真机;去重断言只数 ack 条数不查 ack 归属 | dsh-im fixture 拆协议合约/传输 fake/业务断言三层,心跳 ACK 丢失/迟到/RESUME 各有用例;W13 已修:qq FakeWebSocket 补 error/半帧支路(超时沿用 W8 mock timers 手法)、public fetch mock 补超时支路、mock 分层原则入 TECHNICAL_DEBT.md |
| G-59 | `src/health.mjs`、`src/approval/escalation.mjs`、`src/inbound/pairing.mjs`、`src/admin/ui.mjs` 无同名专门测试,仅被 assembly/index 间接覆盖 | 升级链阶段推进/配对码锁出与 TTL/渠道自检错误形态这类高风险路径无第一手断言;间接覆盖随装配用例增删静默漂移 | dsh-im 对 registration-manager/卡片/连接层均有独立 test 文件;W13 已修:新增 test/health.test.mjs(渠道自检错误形态)/ test/escalation.test.mjs(升级链阶段推进, mock timers)/ test/pairing.test.mjs(配对码锁出与 TTL,按 W11 新语义:过期码不计失败、无效码翻锁) |
| G-60 | README 仅列 /help /whoami /pair /unpair 四条注册命令,会话面 11 条(/status /agent /bind /unbind /stop /route /quiet /unquiet 等)无清单;v0.8.x accountId 来源规则(绝不由 channel 兜底、多账号需显式配置)未文档化(L轮) | 用户从 README 发现不了 /stop /quiet;多账号用户以为 channel 即身份,配错表现为"配对了但不认",文档无对应排障条目 | dsh-im README 命令表带边界说明(私聊可用/不建会话/默认值上限);W13 已修:README/README.zh-CN 补会话面 11 条命令清单(/status /agent /agent use /agent back /bind /unbind /stop /route /quiet /unquiet /help)带边界说明,docs/guide.md 排障表补 accountId 来源规则条目 |
| G-61 | `_engine.mjs:35-56` 已实现 type:'number' 分支,SPEC_CHANNELS 全表零处声明,数值默认仍走 str() 字符串路径(K轮) | 引擎能力与声明表脱节:未来给 spec 加数值 default 忘标 type,默认值先被字符串化再靠调用方 Number() 救回;与 G-32 同族的休眠陷阱 | dsh-im 数值配置统一 parseInt+isSafeInteger |
| G-62 | `pushplus.mjs:26-36` template 非法静默回退 'markdown',channel 非法 warn 后抛 NOT_CONFIGURED | 同一 resolve 内两个可选枚举一字段宽松一字段严格:误写 template 被静默改命、误写 channel 直接禁用渠道,用户无法从行为反推哪份配置被动过 | 竞品枚举校验统一白名单抛错(qmsg/onebot validate) |
| G-63 | `webhook.mjs:15-17`+`config.mjs:55-56` headers 仅排除数组,值不字符串化:{x:123}/嵌套对象原样进 fetch headers | 数组静默丢、非字符串值行为依赖 undici 版本实现(可能抛 TypeError 也可能隐式转换);自定义头"时灵时不灵"且无 warn | 竞品自托管渠道对 header 值逐项 String()+非法键拒绝 |
| G-64 | `desktop.mjs:47-49` sound 仅认 'always'/'never'/'auto' 三字符串:sound:false、sound:'true' 均静默变 'auto' | 布尔习惯写 false 期待静音,实际回落 auto 按级别发声;无 warn 无枚举形态说明 | dsh-im bool() 显式解析 'false'/'0'/'no'/'off' |
| G-65 | `commands.mjs:26-28`(I轮) startsWith('/') 不识别全角 ／pair;注册面命令命中后 parts[1:] 静默丢弃(guided /help 附言只回帮助,附言丢弃) | 中文输入法全角斜杠高频,落普通文本进 agent;命令后附言被吞让"带参求助"变哑——两处都是"解析成功即整条消费"的设计代价 | dsh-im 控制命令用 (?=$\|\s) 边界并把后文完整交给参数捕获 |

---

## 四、安全类

### S-01 · 结构性 · 插件全权限运行，权限声明无强制执行

- **定位**：宿主 `vendor/loader/src/internal.ts:1`（原生模块加载，无沙箱）+ 插件 `package.json:89-99`（dshWorkshop.permissions 仅声明性元数据）
- **易错辨析**：C 轮加证——宿主 loader 实际只读 `id/name/config`（`vendor/loader/src/config/entry.ts:9-16`），permissions 等字段运行时零消费：声明与执行完全脱钩。插件可任意读写文件、执行命令、外发数据，缓解只剩来源信任+锁版本+升级 diff 复审
- **竞品参照**：dsh-im 同宿主同模型，同等暴露

### S-02 · 中危 · webhook/自托管渠道无 SSRF 防护（CWE-918）

- **定位**：`src/adapters/webhook.mjs:10-30`、`src/adapters/_shared.mjs:76-99` —— 直接 fetch，无私网段拦截，无 `redirect:'manual'`；响应片段（≤2048 字符）进错误对象形成探测回显
- **易错辨析**：URL 仅 admin token 持有者/YAML 编辑者可控，定级因此压在中危——但 token 被钓后即成内网跳板，且回显通道让 SSRF 从盲打变明打。易错点：`redirect:'manual'` 的缺失意味着重定向可绕过任何未来加的 URL 校验
- **竞品参照**：dsh-im 无自定义 webhook 渠道，无此面；但其 API 客户端统一 `redirect:'error'`（`telegram-api.mjs:357-363`、`dingtalk-api.mjs:207-213`），重定向显式拒绝有先例可循

### S-03 · 中危 · answer+parallel 审批模式：IM 账号凭据即可放行高危审批（能力设计的安全代价）

- **定位**：`src/approval/router.mjs:68,499-523`、`src/inbound/bus.mjs:231-299` —— answer+parallel 模式下 IM 账号被盗 = 可远程放行 bash 沙箱升级类审批；竞态路径 `ledger.resolve()` 不检查前态
- **易错辨析**：已有缓解厚实（observe 默认、token 单次核销+SHA-256+常量时间比较、来源会话校验、A2 轮专项确认裁决链无 TOCTOU），但 parallel 模式把"设备在身边"的假设换成"IM 账号在手"——安全边界从物理降级到口令。辨析点：这不是实现漏洞而是能力设计的安全代价，审视时应关注高危审批是否该有第二因子
- **竞品参照**：竞品无审批远程裁决面，最小能力反而无此险

### S-04 · 中危 · 各渠道凭证明文落盘 state.json（CWE-312）

- **定位**：`src/inbound/store.mjs:262-276`、`src/admin/api.mjs:910-913`、`src/admin/scan.mjs:187-203`
- **易错辨析**：缓解已到位（0600+原子写+跨进程锁；admin token 例外仅存 hash；GET 全 mask），剩余风险是文件系统级读取（同用户进程、备份同步、误提交）。辨析：D3 轮确认企微 `corpsecret` 走 URL query 是官方 API 固有形态（POST 不可用），日志侧 `_shared` 错误对象不含 URL——"secret 进 URL"的暴露面在协议本身而非实现，与竞品一致
- **竞品参照**：dsh-im 同样明文 JSON 落盘，无 keychain 集成

### S-05 · 中危 · 自动通知默认外发宿主会话数据片段（CWE-200）

- **定位**：`src/event-listener.mjs:16-37,295-309,389`、`src/approval/router.mjs:242-243` —— 输出片段 200 字符/workspace 名/session id/错误全文发往第三方 IM；notify 工具允许 LLM 推任意字符串
- **易错辨析**：默认行为即外发，用户无感知确认；prompt injection 可借 notify 工具构造外发通道。缓解（500 字符上限、工具限流、出站不触发命令）降低可利用性但数据流向本身未告知。辨析：这与"通知插件"的产品本质绑定，审视重点应是默认片段的敏感度而非有无外发
- **竞品参照**：dsh-im 是双向桥,默认即转发会话内容,外发面更大

### S-06 · 中危 · 管理台无 CORS/Origin/CSRF 策略（CWE-352/942）

- **定位**：`src/admin/server.mjs` 全文无 Origin 处理
- **易错辨析**：Bearer 模型（token 在 sessionStorage，浏览器不自动附带）使经典 CSRF 难利用——这是"无策略也安全"的错觉来源；但 host 改 0.0.0.0/反代公网 + token 被钓时缺第二道防线。D2 轮确认请求体 1MB 上限/SSE 64 连接/120s 超时均到位，唯缺 Origin 白名单
- **竞品参照**：dsh-im 无 HTTP 管理面

### S-07 · 中危 · ask_user 远程回答注入 agent 上下文（CWE-74）

- **定位**：`src/questions/router.mjs:461-481,666-703,915-946`、`src/control/entry.mjs:168-216`
- **易错辨析**：回答文本经 token+来源+Control Core 校验后进宿主 agent 会话——校验链管的是"谁答的"，不管"答了什么"；无长度上限无内容过滤。绑定 member（非 owner）即具备作答权。辨析：校验的身份边界与内容的注入边界是两个正交面，前者厚实不等于后者闭合
- **竞品参照**：不适用

### S-08 · 中危 · bootstrap 首绑即 owner 的初始窗口（CWE-284）

- **定位**：`src/inbound/identity.mjs:215-220`、`src/inbound/pairing.mjs:15-48,241-287`
- **易错辨析**：配对码 8 位约 39.6 bit 熵 + 白名单空时首绑即 owner；缓解厚（SHA-256 落盘/10min TTL/单次核销/5 次锁 10min/群聊拒绝/0600+symlink 防护，D2 轮独立复核全部到位）。剩余窗口=码被窥+10 分钟内抢绑。辨析：与 G-20 mint 崩溃窗口叠加时孤儿码延长暴露面
- **竞品参照**：竞品绑定靠手动配置文件，无首绑窗口但可用性差

### S-09 · 低危 · 成员管理端点无角色分层

- **定位**：`src/admin/api.mjs:1006-1060` —— 单 admin token 模型，多人共享无法区分操作者（审计仅 action/detail）
- **易错辨析**：单 token 语义下非越权而是可观测性缺失；团队使用时审计归因失效
- **竞品参照**：不适用

### S-10 · 低危 · 微信扫码即绑定无 owner 二次确认

- **定位**：`src/admin/scan.mjs:323-335` —— 依赖"扫码者专属好友"协议假设
- **易错辨析**：响应被污染或共用设备存在误绑定窗口；非首绑场景无确认。A3 轮专项确认扫码代次/phase/reset 防并发机制完整，缺的只是绑定语义的确认
- **竞品参照**：不适用

### S-11 · 低危 · 调试脚本 hook-server.mjs 无鉴权写 /tmp

- **定位**：`scripts/hook-server.mjs:1-16` —— 127.0.0.1:9999 无鉴权任意 body 追加写 jsonl（512KB cap）
- **易错辨析**：不随运行时启动,风险仅开发机本机;建议随包分发时剔除
- **竞品参照**：不适用
- **W13 状态**：已修 —— `package.json` files 从含整个 `scripts` 目录改为显式列举六个脚本（channel-login/channel-selfcheck/gen-channel-matrix/route/verify-release/wechat-login），排除 `hook-server.mjs`；脚本留本仓供开发用（127.0.0.1 + 512KB cap 语义不变，不随包分发）

### S-12 · 低危 · target-guard 未知渠道 fail-open

- **定位**：`src/inbound/target-guard.mjs:31-36` —— 仅排除明显不属于该渠道的 ID,未知渠道放行
- **易错辨析**：入站有 identity.allows 复合键兜底,纵深缺口而非单点失效;与 G-13 枚举散落同根——枚举漏改时这里是第二道放行
- **竞品参照**：不适用

### S-13 · 低危 · 依赖未锁定 + 一个授权不明依赖

- **定位**：`package.json:39-43` —— optionalDependencies 均 caret 无 lockfile;`@tencent-connect/qqbot-connector@^1.2.0` npm 标记 UNLICENSED
- **易错辨析**：CI 强制 dependencies 为空(零运行时依赖主张成立)、无生命周期钩子,供应链面干净;剩余是 optional 装配面与授权合规
- **竞品参照**：竞品重依赖官方 SDK(依赖树深但授权明确)
- **W13 状态**：已修 —— optionalDependencies 收敛精确版本（`@larksuiteoapi/node-sdk` 锁死已审查版 1.73.0、`qrcode-terminal` 0.12.0），移除 UNLICENSED 的 `@tencent-connect/qqbot-connector`（维持「仅参考不引入」，QQ 扫码缺包降级 missing-sdk 不变）；可选装配语义与 UNLICENSED 声明写入 README/README.zh-CN

### S-14 · 低危 · parallel 审批 ledger 状态机前态不设防

- **定位**：`src/interaction/ledger.mjs:58-63` —— resolve() 不查前态即翻终态
- **易错辨析**：主链路 bus settle 有 already-resolved 保护(A2 轮专项确认裁决链六类竞态全有防线),此处是内部 API 的健壮性缺口,误用才触发
- **竞品参照**：不适用

### S-15 · 信任/信息 · git 历史存在 npm 刷量工作流（已删除）

- **定位**：git 历史 `61a6787`（2026-08-16 删）`.github/workflows/boost-downloads.yml` —— cron 每 2h + 5 IP 并行,每轮最高 6000 次请求 registry.npmjs.org
- **易错辨析**：不影响运行时;违反平台规则,对供应链信任是负面信号,审计时应在 git 历史层面声明
- **竞品参照**：无同类记录

---

## 五、专项轮补充的设计权衡与观察项（非缺陷定性）

以下条目经 A/B/C/D 四轮专项裁决为"设计意图明确/防护到位",列为观察项供后续演进参考,不计入问题统计：

| 条目 | 定位 | 辨析摘要 |
|---|---|---|
| 启动损坏白纸重置 | `store.mjs:38-83` loadBoot 损坏返回 {} | "无记忆好过误清空"为显式策略,留 .corrupt 取证;连锁代价:绑定表+`inbound:migrated` 标记同失,下次启动重播 YAML 白名单,管理台已删成员复活(G-44 的放大场景) |
| 运行时损坏内存重建 | `store.mjs:233-259` | 已正确处理"不能从 {} 起步"的误清空,多轮加固痕迹清晰 |
| 写锁两轮超时降级无锁 | `store.mjs:191-201` | 注释明示"比永远写不进强,窗口毫秒级";两进程整文件丢写的理论窗口,单宿主部署下不触发 |
| rename 原子性 | `store.mjs:262-271` | tmp 与目标同目录(同文件系统),跨 fs 风险不存在——B 轮子代理的跨挂载点担忧经核实不成立 |
| 定时器全量清点 | index/event-listener/rules/tracker/escalation | C1+D1 两轮独立清点:全部有 dispose/clear 路径,热重载无定时器泄漏 |
| 裁决链六类竞态 | bus/approval/questions/ledger/control | A2 轮专项:双设备并发裁决/超时与 settle/ledger 交错/升级提醒/仲裁归属/token 核销 TOCTOU 均有显式防线 |
| 洪泛与畸形输入 | 入站全路径 + admin | D2 轮专项:冒号 userId 显式拒绝(含读取侧)、1MB body 上限、SSE 64 连接、120s 超时、pairing 码正则+锁出,可利用点零 |
| 微信 pending 合并窗 | `conversation.mjs:475` | 每 entry 必有 timer,到期 flush 即删,自愈;仅 mergeWindowMs 配置极端时有意义 |
| session-registry TTL 保留 | `session-registry.mjs:264-301` | disposedAt+ttl 供同 id resume,有 sweep+墓碑防复活;内存驻留是设计代价(与 G-47 的 store 侧无清理是两回事) |
| escalation chains | `escalation.mjs:15,34,64` | start 前置 stop + 双路由 stop 兜底 + dispose,D1 轮担忧经核实不成立 |
| turn/start 尾沿合并 | `event-listener.mjs:428-431` | 注释明示 start→end 同 key 合并只发 end,防通知轰炸的产品意图,非时序漏洞 |
| spec/手写 timeoutMs 双默认来源 | `_engine.mjs:55` vs 各手写 adapter | 同字段两套默认来源(spec.timeoutMs ?? 10000 vs 固定 10000),一致性债,当前值域一致无行为差异 |
| serverchan 三别名隐式优先级 | `serverchan.mjs:13-15` sct>sendKey>sctKey | 并存时静默取前者,无冲突告警;SECRET_FIELDS 三者全覆盖,脱敏面完整 |
| 双 mask 策略并存 | `config.mjs:177-185`(尾4位) vs `admin/api.mjs:146-153`(全***) | 公共 API 与管理台展示粒度不同,前者保留尾4便于辨认;非泄露但策略应统一表述 |
| cordis Config schema 未用 | `plugin-entry.mjs` 无 Config 导出 | 宿主 fiber.resolveConfig 对无 Config 插件直接放行,合法;放弃了 cordis 标准校验机制,配置错误依赖手写 resolve 报错 |
| dshWorkshop 元数据零消费 | `package.json:72-99` | 宿主 loader 只读 id/name/config(S-01 的另一面);声明服务于市场/审计而非运行时 |
| payload 宽容解析层 | `event-listener.mjs:51-61,460-469` | session/event 双形状、agent/disposed 的 `payload.agent ?? payload` 兼容:当前与宿主契约一致,宽容层是未来版本漂移的缓冲也是误读面 |
| 分段部分成功不可见 | `notify.mjs:39-56`+`segment.mjs:65-88` | sent=2/5 时 noRetry 防重复投送正确,但 outcome 只报渠道级失败,用户不知前 2 段已收到 |
| Telegram 出站无 4096 硬限 | `adapters/telegram.mjs` | 依赖默认分段 1200 码点兜底;segment.enabled=false 或 maxCodepoints 调大时暴露平台 400 |
| 前端 SSE 旧 fetch 未 abort | `admin/ui.mjs:1373-1408` | epoch 只拦逻辑回调不拦连接,同 tab 重复启动时旧 fetch 挂至流结束;服务端 64 上限兜底 |
| channelTest 覆盖面 | `health.mjs:18-38` | 真发裸 adapter 消息(非仅配置检查),但不覆盖路由矩阵/重试/分段/facade 限流编排链 |
| rateGate 串行链语义 | `_tokens.mjs:44-56` | chain 串行化对同实例并发有效不漏(D3 确认);gate 是 per-adapter 实例,跨 notifyAll 的全局限速无约束 |

---

## 六、补测缺口清单

1. 钉钉真实帧形状回归：headers.messageId + SYSTEM/ping 回显断言 + "ack 确实发出"断言 + data parse 失败 warn 断言（G-01/02/42）
2. `/stop xxx` 不取消；`parseCommand('/pair@bot X')` name==='pair'（G-04/06）
3. settleQuestion mock appendAudit 抛错仍返回 settled:true（G-05）
4. iLink chunkSize 边界 + 代理对字符串逐块无孤立代理项（G-03/22）
5. 微信同用户同文本 24h 内两次投递（平台缺 rawId 场景）断言第二次不被吞（G-46）
6. close code 4004 → invalidate 被调 + 弃会话；4008 → 固定 60s（G-07）
7. invalidate 后旧 inflight 晚完成不覆盖新 cached（generation 断言）（G-11）
8. bind 切换后旧会话 inbound 挂钩已摘（G-48）
9. ServerChan sctp key 报错文案；三别名并存时的生效优先级（G-09）
10. 真机回归：钉钉挂 10 分钟观察周期断连（验证 G-02）；scoped agent error 插件可收（验证 G-19）；微信发 emoji 长文看分块乱码（验证 G-03）
