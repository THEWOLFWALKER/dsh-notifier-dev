# Workstream: task-09-feishu-telegram

- Agent identity: Claude Fable 5 | volcengine/deepseek-v4-flash | shared Windows workspace
- Agent: `codex/stage3-team-policy`
- Branch: `codex/stage5-wechat-ilink-hardening` (shared worktree; Stage-6 task-09 owned files committed on top)
- Status: done
- Start/end: `2026-08-26 -> active`
- Scope: Stage 6 — wire the Feishu/Telegram provider facades into production assembly, inject one stable per-provider `accountId` into every normalized envelope, and harden Feishu WS lifecycle + Telegram poll/offset/edit semantics without adding provider-local permission/settlement logic or touching another agent's files.
- Plan: (bounded, ordered)
  1. **accountId injection (providers)** — add an `accountId` option to `src/inbound/feishu-bot.mjs` and `src/inbound/telegram-bot.mjs`; inject it into every normalized message envelope (`bus.accept`), and into every callback/action/approval/question envelope passed to `control.handle` / `bus.decide` / `actions.dispatch` / `questions.decide`. Resolver is provider-local and never trusts the event: Telegram = `config.accountId ?? 'default'` (never `botToken`); Feishu = `config.accountId ?? config.appId` (never event-supplied `data.accountId`). Facades pass the resolved `accountId` down into legacy options.
  2. **Facade normalizers fail closed on event accountId** — `normalizeTelegramCallback` / `normalizeFeishuCallback` must not take `accountId` from the event payload; the transport injects its own resolved `accountId`. Keeps Control Core source binding consistent (wrong/absent event account can never mint the source).
  3. **Wire facades into assembly** — `src/assembly/inbound-channels.mjs`: `DEFAULT_FACTORIES.telegram = createTelegramTransport`, `.feishu = createFeishuTransport`; preserve the existing `{ ...DEFAULT_FACTORIES, ...factories }` override merge and the legacy `create*Inbound` exports on the facades. Assembly passes `accountId` through `config` so the facades/legacy both see it.
  4. **Feishu hardening** — truthful WS lifecycle/status via a new `clientState()` in `feishu-bot.mjs` (starting/connected/stopped/unavailable/error) surfaced by the facade `status()`; defensive SDK-missing/error handling already present but reflected in status; group sensitive control cards (`oc_*` chat) downgraded to plain text (no buttons) before send; `editResolved`/`patchResolvedCard` failure emits exactly ONE text fallback to the source chat.
  5. **Telegram hardening** — process-before-commit offset (advance/store `tg:offset` only after `handleUpdate(update)` succeeds so a failed/in-flight control callback is redelivered, never silently dropped; redelivery deduped by bus messageId / control eventId); `editResolved` retries with a fresh `sendMessage` text fallback when both `editMessageText` attempts fail (deleted/edited message); route `sendText` through `clampTelegramText` to preserve the UTF-16 4096 boundary; keep single-use `refs.take` replay-safety; exports `clientState()`-style truth where the facade can consume it.
  6. **Tests** — extend/adversarial: channel-facade tests (factory default uses facade, accountId resolution stable/never-botToken/never-event, override preserved), inbound tests (accountId on every envelope; Feishu group downgrade, patch-fallback-one-text, lifecycle states, wrong-chat/wrong-user/expired/duplicate fail closed; Telegram offset not-advanced-on-failure, edit-resolved text fallback when deleted, UTF-16 4096 for text fallback, file failure never blocks text). Keep capability constants consistent (`fileSend: 'declared'`, `realDeviceVerified: false`) with the README matrix.
  7. **Validate** — focused tests → `npm test` → `node scripts/verify-release.mjs` → `node scripts/gen-channel-matrix.mjs --check` → `node --check src/index.mjs` → `git diff --check`.
  8. **Commit** — narrow Stage-6 commit (force-add `.agents/workstreams/task-09-feishu-telegram.md`); do not stage another agent's files; no public push.
- Owned files: `src/assembly/inbound-channels.mjs`; `src/channels/feishu/index.mjs`; `src/channels/telegram/index.mjs`; `src/inbound/feishu-bot.mjs`; `src/inbound/telegram-bot.mjs`; `test/channels/feishu.test.mjs`; `test/channels/telegram.test.mjs`; `test/inbound.feishu.test.mjs`; `test/inbound.telegram.test.mjs`; `test/assembly-runtime.test.mjs`; `test/wiring.route.test.mjs`; `.agents/workstreams/task-09-feishu-telegram.md` (force-add).
- Do not touch: `src/inbound/store.mjs`, `src/routing/*`, `src/control/*`, `src/admin/*`, `src/channels/wechat-ilink/*`, `package.json`/version, public remotes, `HANDOFF.md`, `CHANGELOG.md`, any other workstream file.
- Validation: focused channel/inbound tests; `npm test`; `node scripts/verify-release.mjs`; `node scripts/gen-channel-matrix.mjs --check`; `node --check src/index.mjs`; `git diff --check`.
- Adversarial review: wrong-chat / wrong-user / expired / duplicate callbacks fail closed; accountId never derived from `botToken` or event-supplied fields; group sensitive control cards downgraded/blocked before send; patch failure emits exactly one text fallback; Telegram offset never silently drops an unaccepted control command (process-before-commit) with redelivery dedup; file failure never blocks text; capability evidence consistent with README matrix.
- Handoff: implemented + validated. DEFAULT_FACTORIES telegram/feishu now route to the provider facades (`channels/*`), which resolve a stable per-provider `accountId` (feishu: `config.accountId ?? config.appId`; telegram: `config.accountId ?? 'default'`, never `botToken`) and feed it into the shared inbound so every normalized message/callback/action/approval/question envelope carries it. Facade normalizers fail closed: `normalize*Callback` never takes accountId from the event. Feishu: truthful `clientState()`→facade `status()` (idle/starting/connected/unavailable/error/stopped); `oc_*` group sensitive control cards (approval/action) downgraded to plain text before send, question cards blocked; `patchResolvedCard`/`editResolved` patch failure emits exactly ONE text fallback to the source chat. Telegram: process-before-commit offset (advance/store only after `handleUpdate` succeeds, `break` on failure → redelivery deduped by messageId/eventId); `editResolved` on both-edit-failure sends one fresh clamped `sendMessage` text; `sendText` routed through `clampTelegramText` (UTF-16 4096, never splits a surrogate pair); lifecycle via `clientState()`. Validation: focused tests pass; `npm test` 1246 total / 1245 pass / 0 fail / 1 skip; verify-release ok (v0.8.6, tests=909 doc guard); channel-matrix `--check` ok (27 渠道); `node --check src/index.mjs` ok; `git diff --check` ok. Owned-file commit (task-09) staged on the shared worktree; no public push, no HANDOFF/CHANGELOG/docs edits. Capability evidence unchanged (`fileSend:'declared'`, `realDeviceVerified:false`).