# Workstream: tech-debt-protocol-guards

- Agent identity: `Trae1 | Trae (SOLO) primary agent | Linux sandbox (Ubuntu 22.04, Node v22.16.0)`
- Agent: `Trae1`
- Branch: `codex/tech-debt-protocol-guards`
- Status: done
- Start/end: `2026-08-20 -> 2026-08-20`
- Scope: P1-1 provider protocol blind spots — add Telegram card-path text clamping (4096-char sendMessage limit) and anti-regression protocol shape guards for the card paths, per `docs/TECHNICAL_DEBT.md` P1-1.
- Plan:
  1. Audit protocol shape risks in card paths (`sendApprovalCard` / `sendActionCard` / `sendQuestionCard`): callback_data length (guarded since v0.6.2), legacy markdown (removed since v0.6.3, no regression guard), text length (unguarded — approval `reason` and question `context` have no upstream cap; a long reason makes Telegram reject sendMessage with 400 `message is too long`, silently killing the button path on all chats and degrading to numbered replies with only a warn line).
  2. Add a code-point-aware clamp helper in `src/inbound/telegram-bot.mjs` applied to every card `text`; truncated output carries a visible marker.
  3. Add focused tests: oversized content still delivers a card (not null), text within 4096 code points, marker present, refs intact; plus parse_mode anti-regression guard (no parse_mode in any card sendMessage).
  4. CHANGELOG entry with the P1-1 / mock-blind-spot reason; record the real-device validation gap in `docs/memory/risks.md`.
- Owned files: `src/inbound/telegram-bot.mjs`, `test/inbound.telegram.test.mjs`, `CHANGELOG.md`, `docs/memory/risks.md`, this workstream file (plus routine HANDOFF/neat-freak sync at closeout).
- Do not touch: outbound `notify.mjs`/segment logic (already bounded at 1200 code points), `http-callback.mjs` (64KB cap already tested), feishu/qq/wxpusher/dingtalk card builders (JSON cards with roomier limits; no evidence of overflow — per P2 rule, no change without evidence), A3–A6 security-plan scope (callback body limits belong to A5), `package.json` version.
- Validation: `node --test test/inbound.telegram.test.mjs` (31/31), full `npm test` (897/897 pass on this Linux host), `node scripts/verify-release.mjs` (ok, documented tests=897), `node scripts/gen-channel-matrix.mjs --check` (ok, 27 channels), `node --check src/index.mjs` + `node --check src/inbound/telegram-bot.mjs`, `git diff --check` (clean).
- Adversarial review: challenged input boundaries (String coercion handles null/undefined), resource use (20000-codepoint cap from public layer is trivially spreadable), token lifecycle (token lives in callback_data refs, never in card text — truncation cannot break redemption), cross-channel bleed (telegram-only file), regression risk (in-limit text passes through unchanged, boundary test), and log leakage (marker carries no sensitive data). **Found and fixed one defect during review: the initial implementation counted Unicode code points, but Telegram enforces the 4096 limit in UTF-16 code units (astral chars are 1 code point but 2 units — a 3000-emoji text is code-point-compliant yet 6000 units and would still 400 on a real device). Reimplemented with code-unit budgeting plus a code-point-boundary cutback so surrogate pairs are never split, and added a dedicated adversarial test for exactly that case.** Not fixed / out of scope by design: feishu/qq/wxpusher/dingtalk JSON cards stay unclamped (no overflow evidence; P2 rule), `editResolved` texts are short fixed literals (no clamp until evidence), real-device boundary confirmation remains open and is recorded in `docs/memory/risks.md`.
- Handoff: P1-1 Telegram card-path slice landed. `clampTelegramText()` in `src/inbound/telegram-bot.mjs` clamps all three card texts to 4096 UTF-16 code units with a visible truncation marker; 6 new protocol-shape tests (approval/question/action overflow, code-unit-vs-code-point adversarial case, in-limit passthrough, parse_mode anti-regression); test contract 891 -> 897 synced across `package.json` `dshQuality.testCount`, both README badges/body text, and the HANDOFF test row; CHANGELOG `[Unreleased]` entry with the P1-1/mock-blind-spot rationale; real-device gap recorded in `docs/memory/risks.md`. Remaining P1-1 work for the next agent: real-device confirmation of the clamp boundary, provider payload limits for other channels pending evidence, long-lived connection lifecycle validation (P1-1 last bullet). Final commit: see git history of this branch.
